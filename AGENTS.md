After using `run` to run opencode in an isolated environment, you can find logs for it inside of the artifacts directory:

The artifacts directory can be read from the output of the `run` command

Backend logs: `%artifacts%/home/.local/share/opencode/log/opencode*.log`
